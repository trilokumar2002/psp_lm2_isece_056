#include<stdio.h>
int add(int a, int b);
int main()
{
int num1, num2, sum;
printf("enter two number to be added: /n");
scanf( "%d %d", &num1, &num2);
sum=add(num1,num2)
printf("sum of%d and %d is %d",num1, num2,sum);
return 0;
}
int add(int a, int b);
{
int result;
result= a+b
return result;
}
